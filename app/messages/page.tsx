import React from "react";

import MessagesScreen from "./main";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <MessagesScreen />
    </div>
  );
};

export default page;
